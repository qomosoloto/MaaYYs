# API 手册

MaaYYs 不提供网络 API。扩展契约主要由 interface.json、tasks/*.json、Pipeline JSON 与 agent 自定义 runner 参数构成。

## GuildBarrierTargetRecognition

用于记录寮突破目标、观察同一目标是否持续停留，以及记录攻击结果。参数以 custom_recognition_param JSON 传入。

